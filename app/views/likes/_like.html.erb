<% if user_signed_in? %>
 <% if post.like_user(current_user.id) %>
  <!-- <%= button_to "/posts/#{post.id}/unlike",params:{index: i}, method: :delete, class: 'like', data: {id: post.id} do %> -->
  <div class="like-button">
   <%= image_tag("icon_red_heart.svg", :id => post.id) %>
   <span class="like-sum">
     <%= post.likes_count %>
   </span>
 </div>
  <!-- <% end %> -->
 <% else %>
  <!-- <%= button_to "/posts/#{post.id}/like",params:{index: i} ,class: 'like', data: {id: post.id} do %> -->
  <div class="like-button">
   <%= image_tag("icon_heart.svg", :id => post.id) %>
   <span class="like-sum">
     <%= post.likes_count %>
   </span>
 </div>
  <!-- <% end %> -->
 <% end %>
<% else %>
<%= image_tag("icon_heart.svg") %>
<span>
  <%= post.likes_count %>
</span>
<% end %>
